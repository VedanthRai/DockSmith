FROM python:3.12-slim

WORKDIR /app

ENV APP_NAME=DocksmithDemo
ENV GREETING=Hello

COPY app.py /app/app.py
COPY data /app/data

RUN echo "Build complete: $APP_NAME is ready"

CMD ["/usr/bin/python3", "/app/app.py"]
